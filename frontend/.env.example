# Browser-facing API base URL. Prefer a same-origin path so credentials and
# deployment hosts are not baked into the frontend bundle.
VITE_API_URL=/api/v1

# Development-only target used by Vite for same-origin /api requests.
VITE_API_PROXY_TARGET=http://localhost:8081
